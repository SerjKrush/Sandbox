Install [Flutter](https://docs.flutter.dev/get-started/install) (version 3.0.0 or newer).
Fetch the project dependencies:
   ```bash
   flutter pub get
   ```
Run the demo on an emulator or device:
   ```bash
   flutter run
